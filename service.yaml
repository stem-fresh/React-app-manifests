apiVersion: v1
kind: Service
metadata:
  name: app-nginx-service
  namespace: irys-ui
spec:
  selector:
    app: irys-app
  ports:
    - name: https
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
